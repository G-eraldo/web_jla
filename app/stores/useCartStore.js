import { defineStore } from 'pinia'
export const useCartStore = defineStore('cart', { state: () => ({ items: [] }), getters: { itemCount: state => state.items.reduce((n, item) => n + item.quantity, 0), total: state => state.items.reduce((n, item) => n + item.quantity * item.price, 0) }, actions: { addItem(product) { const item = this.items.find(entry => entry.id === product.id); if (item) item.quantity++; else this.items.push({ ...product, quantity: 1 }) }, removeItem(id) { this.items = this.items.filter(item => item.id !== id) }, clearCart() { this.items = [] } }, persist: true })
