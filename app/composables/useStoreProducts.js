import { products as demoProducts } from '~/data/products'
export function useStoreProducts() { const config = useRuntimeConfig(); const normalizeProduct = product => { const source = product.images?.[0]?.url || product.image || demoProducts[0].image; return { id: product.documentId || product.id, slug: product.slug, name: product.name, price: Number(product.price), description: typeof product.description === 'string' ? product.description : 'Un bijou Maison JLA imaginé pour illuminer le quotidien.', category: String(product.category || 'Bijou').trim(), categorySlug: String(product.category || 'Bijou').trim().toLowerCase(), image: source.startsWith('/') ? `${config.public.strapiUrl}${source}` : source } }; const listProducts = async () => { try { const { find } = useStrapi(); const response = await find('products', { fields: ['name','slug','price','category','description'], populate: ['images'], pagination: { pageSize: 100 } }); return response.data?.length ? response.data.map(normalizeProduct) : demoProducts.map(normalizeProduct) } catch { return demoProducts.map(normalizeProduct) } }; return { listProducts, normalizeProduct } }
